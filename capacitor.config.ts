import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.waketecnologia.todoionic6',
  appName: 'todo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
